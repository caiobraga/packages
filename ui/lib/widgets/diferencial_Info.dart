/*
import 'package:flutter/cupertino.dart';
import 'package:zeder/design_system/design_system.dart';

class DSDiferencialInfo extends StatelessWidget {
  String get nomeIcone => 'lightning-bolt';
  String get testo => 'Profissionais verificados';
  VoidCallback get onPressed => (){};

  Widget get topinfo => DSIconFilledSecondary(iconName: nomeIcone);
  Widget get bottoninfo => DSTextSubtitlePrimary(text: testo);

  const DSDiferencialInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return DSInfoCard(topinfo: topinfo, bottoninfo: bottoninfo);
  }
}
*/
