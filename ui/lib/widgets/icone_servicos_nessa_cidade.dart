/*
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

import '../../design_system/widgets/icons.dart';
import '../../design_system/widgets/text.dart';

class IconeServicosNessaCidade extends StatelessWidget {
  const IconeServicosNessaCidade({Key? key}) : super(key: key);

  String get nomeIcone => 'lightning-bolt';

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 90,
      width: 72,
      child: Column(
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        crossAxisAlignment: CrossAxisAlignment.center,
        children: [
          DSIconFilledPrimaryLarge(iconName: nomeIcone),
          DSTextTitleBoldSecondary(text: 'Eletricista',),
      ],),
    );
  }
}
*/
